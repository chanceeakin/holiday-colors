import { createTheme } from "../index";

describe("create theme", function () {
  it.todo("returns a theme");
  it.todo("doesnt care about the time of the year");
  it.todo("gracefully errors when passed the wrong object");
  it.todo("returns temporary themes");
});
